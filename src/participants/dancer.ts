export class Dancer {
  name: string;

  constructor(name: string) {
    this.name = name;
  }

  dance() {
    // TODO: update to return round stats
  }

  toString() {
    return `${this.name}`;
  }
}
